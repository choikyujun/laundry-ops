# Memory Flush: 2026-04-07

- **Context:** Laundry Ops v32 SaaS platform development.
- **Problem:** Users reported "login is not defined" error and synchronization issues where UI didn't update after admin approval.
- **Action Taken:**
    - Modified `window.login` to `async` and explicitly called `window.fetchFromSupabase` before re-synchronizing with `platformData` to fix the login race condition.
    - Updated `saveData` to `await syncToSupabase` to ensure server state consistency.
    - Refactored `window.switchTab` to `async` and added explicit data-refresh calls for all admin and super-admin tabs to ensure new data reflects immediately upon tab switching.
    - Fixed UI issues where admin menus (like "Super Admin") weren't appearing/refreshing correctly by forcing data re-fetch on interaction.
- **Current Status:** Critical bugs (login errors, sync lag) resolved. System is now forcing data consistency via async/await flows.
