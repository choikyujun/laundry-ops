# Memory Log: 2026-04-08

- **Project Status:** Migration from JSON-blob-based architecture (`platform_data` table) to SQL-first architecture (direct `factories`, `invoices`, `hotels` queries).
- **Completed Tasks:**
    - Refactored `loadSuperAdminDashboard` to fetch directly from DB.
    - Updated `viewFactoryDetails` to use async DB queries instead of local `platformData` state.
    - Fixed ID duplication check in registration to use SQL queries.
    - Fixed invoice date query logic (`400 Bad Request` error) by using start/end date ranges instead of hardcoded 30/31 days.
    - Enabled '수정' button in Super Admin dashboard.
- **Refactoring Strategy:** Removed `saveData()` and `syncToSupabase()` dependencies for JSON blobs.
- **Next Steps:** Continue converting remaining UI components (e.g., Hotel/Staff management) to DB-direct queries if needed, though most critical paths are now converted.
- **Recent Updates (Post-Flush):**
    - Repaired multiple `login()` function definition errors caused by duplication during refactoring.
    - Fully migrated Admin Dashboard (`loadAdminDashboard`) to DB-direct queries (SQL-first).
    - Fixed "Revenue 0" issue caused by invalid date ranges (e.g., 4월 31일).
    - Implemented 5-month automatic expiry for new factory registration.
    - Added "Edit" functionality to factory list.
    - Fixed chart trend logic (Timezone-safe key generation).
    - Decoupled Trend Chart dropdown from the main dashboard list filter.
    - Fixed "undefined" label in charts by fetching 'name' from DB.

- **Post-Evening Session Update (22:48+ KST):**
    - **Architecture Insight:** Identified why legacy deployed apps (v32/v33) appear to function without errors despite missing the `platform_data` table: they rely on optimistic Local Storage saves with silent DB sync failures. Warned user of high risk of data loss for live users and recommended restoring the legacy table name in Supabase.
    - **Code Update (`app_v35.js`):** Completely disabled `syncToSupabase` (added early `return;`) to prevent v35 from attempting to write to or recreate the legacy JSON blob table.

- **Late Night Session Update (23:00+ KST):**
    - **CRITICAL ARCHITECTURE RULE REINFORCED:** v35 and all future versions MUST unconditionally use direct DB queries (SQL-First). Local JSON blobs (`platformData`) and blanket sync functions are strictly forbidden.
    - **SuperAdmin & Admin UI Fixes:**
        - Fixed `calculateAdminDashStats` to correctly join `invoices` and `hotels` for accurate Top 10 rankings.
        - Restored "View" (보기) button on recent invoices list, rendering individual item details correctly via DB queries (`invoices`, `invoice_items`, `hotels`).
        - Fixed Print (exportInvoicesToPDF) and Send (sendInvoicesToClient) to properly query `invoice_items` instead of the deprecated `items` JSON column, including matrix UI rendering for standard hotels and grouped UI for special hotels.
        - Removed dummy KakaoTalk API messages from the send modal.
    - **Hotel & Pricing Module Migration (SQL-First):**
        - Instructed user to create new Supabase tables: `factory_default_prices`, `hotel_categories`, `hotel_item_prices`.
        - Fully migrated 'Default Price Settings' to direct DB ops (`factory_default_prices`).
        - Fully migrated 'Hotel Management' (List, Add, Edit, Delete) to direct DB ops (`hotels`).
        - Fully migrated 'Hotel Custom Pricing': split into simple UI (standard) and categorized UI (special).
        - Added auto-copy logic: when a hotel's price list is opened for the first time (0 items), it automatically clones the factory's default prices (`factory_default_prices`) into the hotel's specific pricing table (`hotel_item_prices`).
