
## 2026-04-12
- **System Restoration**: Reverted the codebase entirely to `v37` (baseline) after integration failures with consolidated `v38` patches.
- **Bug Fix**: Resolved `400 Bad Request` errors in `addHotelCustomItem` by explicitly enforcing `category_name: '기타'` in Supabase `hotel_item_prices` inserts. Schema requires `category_name` (non-nullable).
- **Process Correction**: Committing to a "Strict Change Policy" at user's request: absolutely no arbitrary refactoring or consolidation; only apply requested fixes directly to current stable codebase.
