## 2026-04-17 작업 내역

### 현장직원 화면
- **보기 버튼 팝업 버그 수정**: 레거시 `loadStaffDashboard`, `loadStaffInvoiceList`(localStorage 기반)가 로컬 ID로 버튼 생성 → `viewInvoiceDetail`에서 DB 조회 실패. 전부 DB 버전으로 교체.
- **발행 목록 50건 페이징**: `_staffInvoiceAllData` 클라이언트 페이징 구현. `◀ 1 2 3 ▶` + 총 건수 표시.
- **보기 팝업 공급가만 표시**: 일반/특수거래처 모두 부가세·총합계 제거, 공급가만 노출.

### 거래처 파트너 화면
- **로그인 후 데이터 안 보임 수정**:
  - `loadHotelDashboard` 레거시 → DB 버전 재작성 (hotels, factories, invoices, invoice_items 별도 쿼리)
  - 월말 날짜 버그: `YYYY-MM-31` 고정 → `new Date(y, m, 0).getDate()`로 실제 말일 계산 (400 에러 원인)
  - `invoice_items` 중첩 조회 실패 → `invoice_id in (...)` 별도 쿼리로 분리
- **정산 리포트 수신함**: `loadHotelReceivedInvoicesList` 3개 중복 → `sent_logs` 테이블 기반 단일 DB 버전으로 통합
- **정산확인 기능**: `sent_logs.is_confirmed` 컬럼 추가. 미확인=빨강 "수신완료", 확인=초록 "확인완료". 상세 팝업에 "정산확인" 버튼 추가.
- **기간 표시**: "YYYY년 M월 정산" 변환 제거 → 원본 `period` 값 그대로 표시
- **Excel 다운로드**: `downloadSentLogExcel` - `viewSentDetail`과 동일한 로직/품목순서 (hotel_item_prices sort_order 기준). 일반=날짜×품목 매트릭스, 특수=카테고리별 그룹.

### 모바일 반응형
- `style.css` 미디어쿼리 정리: `@media` 중첩 버그 제거, `.chart-container overflow:hidden`, `.table-scroll-wrap` 클래스 추가
- 거래처 파트너 화면: 2×2 카드 그리드, 차트 1열, 모달 100% width
- 대표 화면: 툴바 flex-wrap, 탭 이름 축약, 모든 테이블 `table-scroll-wrap`, 각 섹션 `chart-container` 카드로 감쌈
- `dist/style.css` 동기화 완료

### 공통
- `app_v38.js?v=20260417` 버전 태그 업데이트
- SheetJS CDN 추가 (`xlsx@0.18.5`)
