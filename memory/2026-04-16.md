# 2026-04-16 작업 일지

## 완료된 수정 사항 (app_v38.js, final_fixes.js, 거래명세서프로그램v38.html)

1. **전월 대비 성장률 0% 버그** → 해결
   - `calculateAdminDashStats` 중복 정의 3개 → 1개(최종)로 정리
   - `loadAdminDashboard`에서 `runAdminStats()`(미정의 함수) 호출 → `calculateAdminDashStats()`로 수정
   - `final_fixes.js`: 전월 0원 + 이번달 매출 있으면 100% 처리 추가

2. **발송 시 sent_logs 미저장** → 해결
   - `sendInvoicesToClient` 발송 완료 후 `sent_logs` insert 추가

3. **월정산 발송 내역 > 내역확인 "거래처 정보가 없습니다"** → 해결
   - `viewSentDetail` 전체를 `platformData` 레거시 → DB 직접 조회 방식으로 교체
   - `hotel_item_prices`의 `category` 컬럼 없음 → select에서 제거 (400 에러 수정)
   - 특수거래처 2단 그리드: `hotel_item_prices.category_name` 기준으로 재구성
   - 내역확인 팝업 디자인을 발송 팝업과 동일하게 맞춤

4. **신규직원 등록 안됨** → 해결
   - `saveNewStaff` 레거시 → DB insert 방식으로 교체
   - `id: 'st_' + Date.now()` 추가 (not-null constraint 해결)

5. **현장직원 화면 - 삭제 버튼 → 보기 버튼** → 해결
   - `loadStaffInvoiceList`의 삭제 버튼을 `viewInvoiceDetail()` 호출 보기 버튼으로 교체

6. **현장직원 화면 - 거래처 선택 시 품목 안나옴** → 해결
   - `openInvoiceModal`: `hotels.items` JSON → `hotel_item_prices` DB 조회로 교체
   - 특수거래처 카테고리별 그룹 표시 추가

7. **현장직원 화면 - 저장 버튼 먹통** → 해결
   - `saveAndPrintInvoice` 전체 레거시 → DB insert/update 방식으로 교체
   - `invoices.id: 'inv_' + Date.now()`, `invoice_items.id: crypto.randomUUID()` 추가

## 미완료 (이따가 계속)
- **현장직원 화면 - 보기 버튼 클릭 시 팝업이 안보이는 문제**
  - 모달(`invoiceDetailModal`) 열리고 내용도 있음 (확인됨)
  - 원인: `position:fixed`인데 width/height이 100%가 아니라 710x684px으로 제한됨
  - HTML 수정 완료(`width:100%; height:100%; z-index:9999`) 했으나 아직 미확인
  - → 다음 세션에서 확인 필요
